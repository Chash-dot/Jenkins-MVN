pipeline {
  agent none
  options {
    timeout(time: 1, unit: 'HOURS')
  }
  
  stages {
    stage('Checkout')
    {
      agent { label 'dev' }
      steps {
        git branch: 'master' , url: 'https://github.com/Chash-dot/Jenkins-MVN.git'
      }
    }
  }
}
